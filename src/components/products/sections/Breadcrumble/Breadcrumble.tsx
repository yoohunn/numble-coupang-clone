import { IBreadcremble } from '../../../../types/product.interface';

interface IProps {
  breadcrumble?: IBreadcremble;
}

const Breadcrumble = ({ breadcrumble }: IProps) => {
  return <section>{}</section>;
};

export default Breadcrumble;
